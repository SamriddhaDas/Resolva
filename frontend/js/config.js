// Edit this after deploying the backend on Render (or any host).
// Example: window.RESOLVA_API_BASE = "https://resolva-api.onrender.com";
// Leave empty string to call same-origin (local dev via Node proxy).
window.RESOLVA_API_BASE = "https://resolva.onrender.com/api";
